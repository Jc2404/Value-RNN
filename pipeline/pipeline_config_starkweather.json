{
  "run_name": "Starkweather-p0.2",
  "output_root": "runs",
  "train": {
    "enabled": true,
    "name": "Starkweather-agent-p0.2",
    "args": [
      "--eval-period", "5",
      "--num-rollouts", "50",
      "--num-episodes", "201",
      "--batch-size", "32",
      "--learning-rate", "0.001",
      "--num-gradient-steps", "10",
      "--target-period", "10",
      "--epsilon", "0.2",
      "--buffer-capacity", "8192",
      "starkweather",
      "--p_omission", "0.2"
    ]
  },
  "decoder_train": {
    "enabled": true,
    "name": "frozen_decoders",
    "args": [
      "--run_regression",
      "--run_softmax_belief",
      "--period", "20",
      "--end_episode", "200",
      "--probe_num_samples", "10000",
      "--probe_valid_size", "0.2",
      "--probe_epochs", "300",
      "--probe_lr", "0.001",
      "--probe_batch_size", "1024",
      "--sm_use_mlp",
      "--belief_loss", "kl"
    ]
  },
  "mi_train": {
    "enabled": true,
    "name": "mine_probe",
    "mine_id": "mine_probe",
    "args": [
      "--agent_episode_start", "0",
      "--agent_episode_end", "200",
      "--agent_episode_step", "20",
      "--mine_num_samples", "10000",
      "--mine_num_layers", "2",
      "--mine_hidden_size", "256",
      "--mine_alpha", "0.01",
      "--mine_num_epochs", "400",
      "--mine_batch_size", "1024",
      "--mine_learning_rate", "0.001",
      "--valid_size", "0.2",
      "--use_wandb",
      "--eval_after_train"
    ]
  },
  "protocol_b": {
    "enabled": true,
    "name": "protocolB",
    "args": [
      "--run_mi",
      "--run_regression",
      "--run_softmax_linear_probe",
      "--run_softmax_mlp_probe",
      "--run_belief_eval",
      "--num_samples", "10000",
      "--period", "40",
      "--end_episode", "200",
      "--belief_total_steps", "4000",
      "--valid_size", "0.2",
      "--probe_epochs", "300",
      "--probe_lr", "0.001",
      "--probe_batch_size", "1024",
      "--mine_num_epochs", "400",
      "--mine_batch_size", "1024",
      "--mine_learning_rate", "0.001"
    ],
    "tests": [
      {
        "label": "test_p_omission",
        "flag": "--test_p_omission"
      }
    ]
  }
}
