{
  "run_name": "Starkweather-p0.1",
  "output_root": "runs",
  "train": {
    "enabled": true,
    "name": "Starkweather-agent-p0.1",
    "args": [
      "--eval-period", "5",
      "--num-rollouts", "50",
      "--num-episodes", "201",
      "--batch-size", "32",
      "--learning-rate", "0.001",
      "--num-gradient-steps", "10",
      "--target-period", "10",
      "--epsilon", "0.2",
      "--buffer-capacity", "8192",
      "starkweather",
      "--p_omission", "0.1"
    ]
  },
  "decoder_train": {
    "enabled": true,
    "name": "frozen_decoders",
    "args": [
      "--run_regression",
      "--run_softmax_belief",
      "--period", "20",
      "--end_episode", "200",
      "--probe_num_samples", "10000",
      "--probe_valid_size", "0.2",
      "--probe_epochs", "300",
      "--probe_lr", "0.001",
      "--probe_batch_size", "1024",
      "--sm_use_mlp",
      "--belief_loss", "kl"
    ]
  },
  "protocol_a": {
    "enabled": true,
    "name": "protocolA",
    "args": [
      "--run_softmax_linear_probe",
      "--run_softmax_mlp_probe",
      "--period", "40",
      "--end_episode", "200",
      "--probe_num_samples", "10000",
      "--probe_valid_size", "0.2",
      "--probe_epochs", "300",
      "--probe_lr", "0.001",
      "--probe_batch_size", "1024",
      "--mine_num_samples", "10000",
      "--mine_num_epochs", "400",
      "--mine_batch_size", "1024",
      "--mine_learning_rate", "0.001"
    ],
    "tests": [
      {
        "label": "test_p_omission",
        "flag": "--test_p_omission"
      }
    ]
  },
  "protocol_b": {
    "enabled": true,
    "name": "protocolB",
    "args": [
      "--run_softmax_linear_probe",
      "--run_softmax_mlp_probe",
      "--num_samples", "10000",
      "--period", "40",
      "--end_episode", "200",
      "--belief_total_steps", "4000",
      "--valid_size", "0.2",
      "--probe_epochs", "300",
      "--probe_lr", "0.001",
      "--probe_batch_size", "1024",
      "--mine_num_epochs", "400",
      "--mine_batch_size", "1024",
      "--mine_learning_rate", "0.001"
    ],
    "tests": [
      {
        "label": "test_p_omission",
        "flag": "--test_p_omission"
      }
    ]
  }
}
